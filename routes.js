const HOME = "/";
const LOGIN = "/login";
const SIGN_UP = "/sign_up";
const RANK = "/rank";

const routes = {
    home: HOME,
    login: LOGIN,
    sign_up: SIGN_UP,
    rank: RANK
}

export default routes;